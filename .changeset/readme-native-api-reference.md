---
"blume": patch
---

Update the README's API-reference bullet to lead with the native renderer. Since the 0.5.0 rebuild the default is Blume's own reference (one real page per operation, woven into sidebar/search/`llms.txt`); Scalar is the opt-in embed (and the AsyncAPI path) with the "Try it" playground. The bullet still described the pre-0.5.0 Scalar-only behavior.
