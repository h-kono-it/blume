---
"blume": patch
---

Add the `Feedback` layout slot to the customization docs' wired-slots table. The slot is real and wired (it replaces the "Was this page helpful?" rating, and `blume add feedback` scaffolds an override for it) but the table — which presents itself as the complete list — omitted it.
