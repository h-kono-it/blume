---
"blume": patch
---

The README's CONTRIBUTING link now uses an absolute GitHub URL so it no longer 404s on the npm package page, and the opening tagline no longer repeats the "drop Markdown into a folder" pitch that the very next paragraph makes in full.
