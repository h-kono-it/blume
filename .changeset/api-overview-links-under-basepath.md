---
"blume": patch
---

API overview operation cards now link to the operation pages' routes under the site-wide `basePath`, matching where the content pipeline actually mounts them, so the cards no longer 404 when a `basePath` is configured.
