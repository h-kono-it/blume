---
"blume": patch
---

The CLI reference now quotes the actual error `blume build` prints when a `blume dev` server holds the `.blume` runtime — the previous wording was stale and no longer matched the message.
