---
"blume": patch
---

Clicking "Export to EPUB" again while a generation was in flight could leave the menu item permanently reading "Generating…" until reload — the second click captured the in-progress label as the text to restore. The original label is now remembered once, so the button always returns to its proper label.
