---
"blume": patch
---

Ask AI grounding excerpts now always contain the matched query term: when the remaining context budget shrinks the excerpt window below the 160-character lead-in, the lead-in is capped so the window no longer ends before the match and injects an irrelevant snippet.
