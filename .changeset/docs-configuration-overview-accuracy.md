---
"blume": patch
---

The configuration overview now names the edit page action by its actual UI label (**Edit on GitHub**) and no longer implies `search` supports only two providers — Algolia and the other providers get a mention.
