---
"blume": patch
---

`blume eject`'s success message now prints run commands matching your package manager (detected the same way as `blume init`) instead of always suggesting `bun run dev` and `bun run build`.
